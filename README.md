# TCP Chat Server

A TCP-based client-server chat application implemented in Python.

## Current Features

- Supports multiple concurrent clients using a thread-per-connection server model.
- Broadcasts messages between connected clients and maintains in-memory chat history.

## Tech

- Python
- TCP sockets
- Multithreading